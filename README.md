# **Задача :**
*Написать программу, которая из имеющегося массива строк формирует массив из строк, длина которых меньше либо равна 3 символам. Первоначальный массив можно ввести с клавиатуры, либо задать на старте выполнения алгоритма. При решении не рекомендуется пользоваться коллекциями, лучше обойтись исключительно массивами.*

### Описание алгоритма решения:

1. Объявляем два массива: изначальный "StartArray" и второй такой же длины "FinishArray". 
2. Используем метод, в котором применяем цикл. Внутри цикла проверяем условие, длинна строки массива должна быть меньше или равна трем символам. 
* Если да,то элемент массива "StartArray" заносится в "count" элемент массива "FinishArray". Переменная count используется, чтобы поочередно перемещать элементы из массива "StartArray" в массив "FinishArray". После присвоения переменная "count" увеличивается на 1, и возвращаемся к циклу, в котором "i" увеличивается на 1. 
* Если нет,то возвращаемся к циклу, в котором i увеличивается на 1.
* И так проверяется до последнего элемента массива включительно.
3. После того, как будет проверена длинна крайней строки массива "StartArray", выводим массив с результатом.


Блок-схема данного агоритми находится в корневой папке, файл Final Task.jpg